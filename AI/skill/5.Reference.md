# Reference

Reference 是条件触发，也就是在 Skill 里面用自然语言写逻辑，最终去读取指定的文件

## 例

一个用于解读代码片段的 Skill

Skill 内容：仅当代码中涉及 el 前缀的组件时，需要读取 el-desc.md 并且给每个组件增加注解

## 总结

这个场景里 el-desc.md 就是 Reference

当组件命中 el 前缀，模型才会读取 el-desc.md 文件

最终也是节省了 Token